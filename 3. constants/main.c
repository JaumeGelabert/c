#include <stdio.h>

int main()
{

    const int NUMBER = 3;
    long int longNumber = 1000;

    printf("El valor de la constante es %d\n", NUMBER);
    printf("El valor más grande es %ld\n", longNumber);

    return 0;
}